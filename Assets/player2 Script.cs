using UnityEngine;

public class player2Script : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("player2desu");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
